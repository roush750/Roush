# The current production POS keeps its existing Firebase client configuration
# inside public/pos/index.html during this compatibility migration.
# Move secrets/config to Next.js environment variables only during a later
# intentional Firebase/React migration.
